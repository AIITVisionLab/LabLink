---
header: LabLink实验室管理平台V1.0.0
---

1| ```java
2| public class JwtTokenProvider {
3|
4|     @Value("${app.jwt.secret}")
5|     private String jwtSecret;
6|
7|     @Value("${app.jwt.expiration-ms}")
8|     private long jwtExpirationMs;
9|
10|     public String generateToken(UserDetails userDetails) {
11|         Map<String, Object> claims = new HashMap<>();
12|         return doGenerateToken(claims, userDetails.getUsername());
13|     }
14|
15|     private String doGenerateToken(Map<String, Object> claims, String subject) {
16|         return Jwts.builder()
17|                 .setClaims(claims)
18|                 .setSubject(subject)
19|                 .setIssuedAt(new Date(System.currentTimeMillis()))
20|                 .setExpiration(new Date(System.currentTimeMillis() + jwtExpirationMs))
21|                 .signWith(SignatureAlgorithm.HS512, jwtSecret)
22|                 .compact();
23|     }
24|
25|     public String getUsernameFromToken(String token) {
26|         Claims claims = Jwts.parser()
27|                 .setSigningKey(jwtSecret)
28|                 .parseClaimsJws(token)
29|                 .getBody();
30|         return claims.getSubject();
31|     }
32|
33|     public boolean validateToken(String token) {
34|         try {
35|             Jwts.parser().setSigningKey(jwtSecret).parseClaimsJws(token);
36|             return true;
37|         } catch (SignatureException e) {
38|             logger.error("Invalid JWT signature: {}", e.getMessage());
39|         } catch (MalformedJwtException e) {
40|             logger.error("Invalid JWT token: {}", e.getMessage());
41|         } catch (ExpiredJwtException e) {
42|             logger.error("JWT token is expired: {}", e.getMessage());
43|         } catch (UnsupportedJwtException e) {
44|             logger.error("JWT token is unsupported: {}", e.getMessage());
45|         } catch (IllegalArgumentException e) {
46|             logger.error("JWT claims string is empty: {}", e.getMessage());
47|         }
48|         return false;
49|     }
50| }
51| ```
52|
53| ```java
54| @RestController
55| @RequestMapping("/api/auth")
56| public class AuthController {
57|
58|     @Autowired
59|     private AuthenticationManager authenticationManager;
60|
61|     @Autowired
62|     private JwtTokenProvider tokenProvider;
63|
64|     @Autowired
65|     private UserDetailsService userDetailsService;
66|
67|     @PostMapping("/login")
68|     public Result<Map<String, Object>> login(@RequestBody LoginRequest loginRequest) {
69|         Authentication authentication = authenticationManager.authenticate(
70|                 new UsernamePasswordAuthenticationToken(
71|                         loginRequest.getUsername(),
72|                         loginRequest.getPassword()
73|                 )
74|         );
75|
76|         SecurityContextHolder.getContext().setAuthentication(authentication);
77|         String token = tokenProvider.generateToken((UserDetails) authentication.getPrincipal());
78|         UserDetails userDetails = userDetailsService.loadUserByUsername(loginRequest.getUsername());
79|
80|         Map<String, Object> response = new HashMap<>();
81|         response.put("token", token);
82|         response.put("userInfo", userDetails);
83|         return Result.success(response);
84|     }
85|
86|     @GetMapping("/me")
87|     public Result<UserDetails> getCurrentUser() {
88|         Authentication authentication = SecurityContextHolder.getContext().getAuthentication();
89|         return Result.success((UserDetails) authentication.getPrincipal());
90|     }
91|
92|     @PostMapping("/logout")
93|     public Result<Void> logout() {
94|         SecurityContextHolder.clearContext();
95|         return Result.success();
96|     }
97| }
98| ```
99|
100| ```java
101| @Data
102| public class LoginRequest {
103|     private String username;
104|     private String password;
105| }
106| ```
107|
108| ```java
109| @Data
110| public class Result<T> {
111|     private int code;
112|     private String message;
113|     private T data;
114|
115|     private Result(int code, String message, T data) {
116|         this.code = code;
117|         this.message = message;
118|         this.data = data;
119|     }
120|
121|     public static <T> Result<T> success() {
122|         return new Result<>(200, "success", null);
123|     }
124|
125|     public static <T> Result<T> success(T data) {
126|         return new Result<>(200, "success", data);
127|     }
128|
129|     public static <T> Result<T> error(String message) {
130|         return new Result<>(500, message, null);
131|     }
132|
133|     public static <T> Result<T> error(int code, String message) {
134|         return new Result<>(code, message, null);
135|     }
136| }
137| ```
138|
139| ```vue
140| <template>
141|   <div class="attendance-container">
142|     <el-card class="attendance-card">
143|       <template #header>
144|         <div class="attendance-header">
145|           <h2>考勤管理</h2>
146|           <el-button type="primary" @click="openCreateDialog">创建考勤</el-button>
147|         </div>
148|       </template>
149|       <el-table :data="attendanceList" style="width: 100%">
150|         <el-table-column prop="id" label="考勤ID" width="100" />
151|         <el-table-column prop="title" label="考勤标题" />
152|         <el-table-column prop="startTime" label="开始时间" width="180">
153|           <template #default="{ row }">
154|             {{ formatDateTime(row.startTime) }}
155|           </template>
156|         </el-table-column>
157|         <el-table-column prop="endTime" label="结束时间" width="180">
158|           <template #default="{ row }">
159|             {{ formatDateTime(row.endTime) }}
160|           </template>
161|         </el-table-column>
162|         <el-table-column prop="status" label="状态" width="100">
163|           <template #default="{ row }">
164|             <el-tag :type="row.status === 'ACTIVE' ? 'success' : 'info'">
165|               {{ row.status === 'ACTIVE' ? '进行中' : '已结束' }}
166|             </el-tag>
167|           </template>
168|         </el-table-column>
169|         <el-table-column label="操作" width="150">
170|           <template #default="{ row }">
171|             <el-button size="small" @click="viewDetail(row.id)">查看</el-button>
172|             <el-button size="small" type="danger" @click="endAttendance(row.id)" v-if="row.status === 'ACTIVE'">结束</el-button>
173|           </template>
174|         </el-table-column>
175|       </el-table>
176|       <div class="pagination" v-if="total > 0">
177|         <el-pagination
178|           v-model:current-page="currentPage"
179|           v-model:page-size="pageSize"
180|           :page-sizes="[10, 20, 50, 100]"
181|           layout="total, sizes, prev, pager, next, jumper"
182|           :total="total"
183|           @size-change="handleSizeChange"
184|           @current-change="handleCurrentChange"
185|         />
186|       </div>
187|     </el-card>
188|
189|     <!-- 创建考勤对话框 -->
190|     <el-dialog v-model="createDialogVisible" title="创建考勤">
191|       <el-form :model="createForm" :rules="createRules" ref="createFormRef" label-width="100px">
192|         <el-form-item label="考勤标题" prop="title">
193|           <el-input v-model="createForm.title" placeholder="请输入考勤标题" />
194|         </el-form-item>
195|         <el-form-item label="开始时间" prop="startTime">
196|           <el-date-picker v-model="createForm.startTime" type="datetime" placeholder="选择开始时间" style="width: 100%" />
197|         </el-form-item>
198|         <el-form-item label="结束时间" prop="endTime">
199|           <el-date-picker v-model="createForm.endTime" type="datetime" placeholder="选择结束时间" style="width: 100%" />
200|         </el-form-item>
201|         <el-form-item label="考勤地点" prop="location">
202|           <el-input v-model="createForm.location" placeholder="请输入考勤地点" />
203|         </el-form-item>
204|       </el-form>
205|       <template #footer>
206|         <span class="dialog-footer">
207|           <el-button @click="createDialogVisible = false">取消</el-button>
208|           <el-button type="primary" @click="submitCreate">确定</el-button>
209|         </span>
210|       </template>
211|     </el-dialog>
212|   </div>
213| </template>
214|
215| <script setup>
216| import { ref, reactive, onMounted } from 'vue'
217| import { ElMessage } from 'element-plus'
218| import dayjs from 'dayjs'
219| import { createAttendance, getAttendanceList, endAttendance as endAttendanceApi } from '@/api/attendance'
220|
221| const attendanceList = ref([])
222| const total = ref(0)
223| const currentPage = ref(1)
224| const pageSize = ref(10)
225| const createDialogVisible = ref(false)
226| const createFormRef = ref(null)
227| const createForm = reactive({
228|   title: '',
229|   startTime: new Date(),
230|   endTime: new Date(Date.now() + 60 * 60 * 1000),
231|   location: ''
232| })
233|
234| const createRules = {
235|   title: [
236|     { required: true, message: '请输入考勤标题', trigger: 'blur' }
237|   ],
238|   startTime: [
239|     { required: true, message: '请选择开始时间', trigger: 'change' }
240|   ],
241|   endTime: [
242|     { required: true, message: '请选择结束时间', trigger: 'change' }
243|   ],
244|   location: [
245|     { required: true, message: '请输入考勤地点', trigger: 'blur' }
246|   ]
247| }
248|
249| const loadAttendanceList = async () => {
250|   try {
251|     const response = await getAttendanceList({
252|       page: currentPage.value,
253|       size: pageSize.value
254|     })
255|     attendanceList.value = response.data.list
256|     total.value = response.data.total
257|   } catch (error) {
258|     ElMessage.error('获取考勤列表失败')
259|   }
260| }
261|
262| const handleSizeChange = (size) => {
263|   pageSize.value = size
264|   loadAttendanceList()
265| }
266|
267| const handleCurrentChange = (current) => {
268|   currentPage.value = current
269|   loadAttendanceList()
270| }
271|
272| const openCreateDialog = () => {
273|   createDialogVisible.value = true
274| }
275|
276| const submitCreate = async () => {
277|   if (!createFormRef.value) return
278|   await createFormRef.value.validate(async (valid) => {
279|     if (valid) {
280|       try {
281|         await createAttendance(createForm)
282|         ElMessage.success('考勤创建成功')
283|         createDialogVisible.value = false
284|         loadAttendanceList()
285|       } catch (error) {
286|         ElMessage.error('考勤创建失败')
287|       }
288|     }
289|   })
290| }
291|
292| const viewDetail = (id) => {
293|   // 跳转到考勤详情页
294|   router.push(`/admin/attendance-tasks/${id}`)
295| }
296|
297| const endAttendance = async (id) => {
298|   try {
299|     await endAttendanceApi(id)
300|     ElMessage.success('考勤已结束')
301|     loadAttendanceList()
302|   } catch (error) {
303|     ElMessage.error('结束考勤失败')
304|   }
305| }
306|
307| const formatDateTime = (date) => {
308|   return date ? dayjs(date).format('YYYY-MM-DD HH:mm:ss') : '-'
309| }
310|
311| onMounted(() => {
312|   loadAttendanceList()
313| })
314| </script>
315|
316| <style scoped>
317| .attendance-container {
318|   padding: 16px;
319| }
320|
321| .attendance-header {
322|   display: flex;
323|   justify-content: space-between;
324|   align-items: center;
325|   margin-bottom: 20px;
326| }
327|
328| .attendance-header h2 {
329|   margin: 0;
330|   color: #1b1f2a;
331| }
332|
333| .pagination {
334|   margin-top: 20px;
335|   display: flex;
336|   justify-content: flex-end;
337| }
338| </style>
339| ```
340|
341| ```vue
342| <template>
343|   <div class="lab-container">
344|     <el-card class="lab-card">
345|       <template #header>
346|         <div class="lab-header">
347|           <h2>实验室管理</h2>
348|           <el-button type="primary" @click="openCreateDialog">创建实验室</el-button>
349|         </div>
350|       </template>
351|       <el-input
352|         v-model="searchQuery"
353|         placeholder="搜索实验室名称"
354|         prefix-icon="el-icon-search"
355|         style="margin-bottom: 16px"
356|         @keyup.enter="loadLabList"
357|       />
358|       <el-table :data="labList" style="width: 100%">
359|         <el-table-column prop="id" label="实验室ID" width="100" />
360|         <el-table-column prop="name" label="实验室名称" />
361|         <el-table-column prop="collegeName" label="所属学院" width="150" />
362|         <el-table-column prop="managerName" label="负责人" width="120" />
363|         <el-table-column prop="status" label="状态" width="100">
364|           <template #default="{ row }">
365|             <el-tag :type="row.status === 'ACTIVE' ? 'success' : 'info'">
366|               {{ row.status === 'ACTIVE' ? '启用' : '禁用' }}
367|             </el-tag>
368|           </template>
369|         </el-table-column>
370|         <el-table-column label="操作" width="180">
371|           <template #default="{ row }">
372|             <el-button size="small" @click="viewDetail(row.id)">查看</el-button>
373|             <el-button size="small" type="primary" @click="editLab(row.id)">编辑</el-button>
374|             <el-button size="small" type="danger" @click="deleteLab(row.id)">删除</el-button>
375|           </template>
376|         </el-table-column>
377|       </el-table>
378|       <div class="pagination" v-if="total > 0">
379|         <el-pagination
380|           v-model:current-page="currentPage"
381|           v-model:page-size="pageSize"
382|           :page-sizes="[10, 20, 50, 100]"
383|           layout="total, sizes, prev, pager, next, jumper"
384|           :total="total"
385|           @size-change="handleSizeChange"
386|           @current-change="handleCurrentChange"
387|         />
388|       </div>
389|     </el-card>
390|
391|     <!-- 创建实验室对话框 -->
392|     <el-dialog v-model="createDialogVisible" title="创建实验室">
393|       <el-form :model="createForm" :rules="createRules" ref="createFormRef" label-width="100px">
394|         <el-form-item label="实验室名称" prop="name">
395|           <el-input v-model="createForm.name" placeholder="请输入实验室名称" />
396|         </el-form-item>
397|         <el-form-item label="所属学院" prop="collegeId">
398|           <el-select v-model="createForm.collegeId" placeholder="选择学院">
399|             <el-option v-for="college in colleges" :key="college.id" :label="college.name" :value="college.id" />
400|           </el-select>
401|         </el-form-item>
402|         <el-form-item label="负责人" prop="managerId">
403|           <el-select v-model="createForm.managerId" placeholder="选择负责人">
404|             <el-option v-for="teacher in teachers" :key="teacher.id" :label="teacher.name" :value="teacher.id" />
405|           </el-select>
406|         </el-form-item>
407|         <el-form-item label="实验室描述" prop="description">
408|           <el-input v-model="createForm.description" type="textarea" placeholder="请输入实验室描述" />
409|         </el-form-item>
410|       </el-form>
411|       <template #footer>
412|         <span class="dialog-footer">
413|           <el-button @click="createDialogVisible = false">取消</el-button>
414|           <el-button type="primary" @click="submitCreate">确定</el-button>
415|         </span>
416|       </template>
417|     </el-dialog>
418|   </div>
419| </template>
420|
421| <script setup>
422| import { ref, reactive, onMounted } from 'vue'
423| import { ElMessage } from 'element-plus'
424| import { getLabList, createLab, deleteLab as deleteLabApi } from '@/api/lab'
425| import { getColleges } from '@/api/colleges'
426| import { getTeachers } from '@/api/teachers'
427|
428| const labList = ref([])
429| const total = ref(0)
430| const currentPage = ref(1)
431| const pageSize = ref(10)
432| const searchQuery = ref('')
433| const createDialogVisible = ref(false)
434| const createFormRef = ref(null)
435| const colleges = ref([])
436| const teachers = ref([])
437|
438| const createForm = reactive({
439|   name: '',
440|   collegeId: '',
441|   managerId: '',
442|   description: ''
443| })
444|
445| const createRules = {
446|   name: [
447|     { required: true, message: '请输入实验室名称', trigger: 'blur' }
448|   ],
449|   collegeId: [
450|     { required: true, message: '请选择所属学院', trigger: 'change' }
451|   ],
452|   managerId: [
453|     { required: true, message: '请选择负责人', trigger: 'change' }
454|   ]
455| }
456|
457| const loadLabList = async () => {
458|   try {
459|     const response = await getLabList({
460|       page: currentPage.value,
461|       size: pageSize.value,
462|       name: searchQuery.value
463|     })
464|     labList.value = response.data.list
465|     total.value = response.data.total
466|   } catch (error) {
467|     ElMessage.error('获取实验室列表失败')
468|   }
469| }
470|
471| const loadColleges = async () => {
472|   try {
473|     const response = await getColleges()
474|     colleges.value = response.data
475|   } catch (error) {
476|     ElMessage.error('获取学院列表失败')
477|   }
478| }
479|
480| const loadTeachers = async () => {
481|   try {
482|     const response = await getTeachers()
483|     teachers.value = response.data
484|   } catch (error) {
485|     ElMessage.error('获取教师列表失败')
486|   }
487| }
488|
489| const handleSizeChange = (size) => {
490|   pageSize.value = size
491|   loadLabList()
492| }
493|
494| const handleCurrentChange = (current) => {
495|   currentPage.value = current
496|   loadLabList()
497| }
498|
499| const openCreateDialog = () => {
500|   createDialogVisible.value = true
501| }
502|
503| const submitCreate = async () => {
504|   if (!createFormRef.value) return
505|   await createFormRef.value.validate(async (valid) => {
506|     if (valid) {
507|       try {
508|         await createLab(createForm)
509|         ElMessage.success('实验室创建成功')
510|         createDialogVisible.value = false
511|         loadLabList()
512|       } catch (error) {
513|         ElMessage.error('实验室创建失败')
514|       }
515|     }
516|   })
517| }
518|
519| const viewDetail = (id) => {
520|   // 跳转到实验室详情页
521|   router.push(`/admin/labs/${id}`)
522| }
523|
524| const editLab = (id) => {
525|   // 跳转到编辑页面
526|   router.push(`/admin/labs/${id}/edit`)
527| }
528|
529| const deleteLab = async (id) => {
530|   try {
531|     await deleteLabApi(id)
532|     ElMessage.success('实验室删除成功')
533|     loadLabList()
534|   } catch (error) {
535|     ElMessage.error('实验室删除失败')
536|   }
537| }
538|
539| onMounted(async () => {
540|   await loadColleges()
541|   await loadTeachers()
542|   await loadLabList()
543| })
544| </script>
545|
546| <style scoped>
547| .lab-container {
548|   padding: 16px;
549| }
550|
551| .lab-header {
552|   display: flex;
553|   justify-content: space-between;
554|   align-items: center;
555|   margin-bottom: 20px;
556| }
557|
558| .lab-header h2 {
559|   margin: 0;
560|   color: #1b1f2a;
561| }
562|
563| .pagination {
564|   margin-top: 20px;
565|   display: flex;
566|   justify-content: flex-end;
567| }
568| </style>
569| ```
570|
571| ```java
572| @RestController
573| @RequestMapping("/api/lab")
574| public class LabController {
575|
576|     @Autowired
577|     private LabService labService;
578|
579|     @GetMapping
580|     public Result<PageInfo<LabDto>> getLabList(
581|             @RequestParam(defaultValue = "1") Integer page,
582|             @RequestParam(defaultValue = "10") Integer size,
583|             @RequestParam(required = false) String name) {
584|         PageInfo<LabDto> pageInfo = labService.getLabList(page, size, name);
585|         return Result.success(pageInfo);
586|     }
587|
588|     @GetMapping("/{id}")
589|     public Result<LabDto> getLabDetail(@PathVariable Long id) {
590|         LabDto labDto = labService.getLabDetail(id);
591|         return Result.success(labDto);
592|     }
593|
594|     @PostMapping
595|     public Result<Void> createLab(@RequestBody LabCreateDto labCreateDto) {
596|         labService.createLab(labCreateDto);
597|         return Result.success();
598|     }
599|
600|     @PutMapping("/{id}")
601|     public Result<Void> updateLab(@PathVariable Long id, @RequestBody LabUpdateDto labUpdateDto) {
602|         labService.updateLab(id, labUpdateDto);
603|         return Result.success();
604|     }
605|
606|     @DeleteMapping("/{id}")
607|     public Result<Void> deleteLab(@PathVariable Long id) {
608|         labService.deleteLab(id);
609|         return Result.success();
610|     }
611| }
612| ```
613|
614| ```java
615| @Service
616| public class LabService {
617|
618|     @Autowired
619|     private LabMapper labMapper;
620|
621|     @Autowired
622|     private CollegeMapper collegeMapper;
623|
624|     @Autowired
625|     private UserMapper userMapper;
626|
627|     public PageInfo<LabDto> getLabList(Integer page, Integer size, String name) {
628|         PageHelper.startPage(page, size);
629|         List<Lab> labs = labMapper.selectList(new QueryWrapper<Lab>().like(StringUtils.isNotBlank(name), "name", name));
630|         PageInfo<Lab> pageInfo = new PageInfo<>(labs);
631|
632|         List<LabDto> labDtos = labs.stream().map(lab -> {
633|             LabDto dto = new LabDto();
634|             BeanUtils.copyProperties(lab, dto);
635|             
636|             // 获取学院名称
637|             College college = collegeMapper.selectById(lab.getCollegeId());
638|             if (college != null) {
639|                 dto.setCollegeName(college.getName());
640|             }
641|
642|             // 获取负责人名称
643|             User manager = userMapper.selectById(lab.getManagerId());
644|             if (manager != null) {
645|                 dto.setManagerName(manager.getName());
646|             }
647|
648|             return dto;
649|         }).collect(Collectors.toList());
650|
651|         PageInfo<LabDto> result = new PageInfo<>();
652|         BeanUtils.copyProperties(pageInfo, result);
653|         result.setList(labDtos);
654|         return result;
655|     }
656|
657|     public LabDto getLabDetail(Long id) {
658|         Lab lab = labMapper.selectById(id);
659|         if (lab == null) {
660|             throw new RuntimeException("实验室不存在");
661|         }
662|
663|         LabDto dto = new LabDto();
664|         BeanUtils.copyProperties(lab, dto);
665|
666|         // 获取学院名称
667|         College college = collegeMapper.selectById(lab.getCollegeId());
668|         if (college != null) {
669|             dto.setCollegeName(college.getName());
670|         }
671|
672|         // 获取负责人名称
673|         User manager = userMapper.selectById(lab.getManagerId());
674|         if (manager != null) {
675|             dto.setManagerName(manager.getName());
676|         }
677|
678|         return dto;
679|     }
680|
681|     public void createLab(LabCreateDto labCreateDto) {
682|         Lab lab = new Lab();
683|         BeanUtils.copyProperties(labCreateDto, lab);
684|         lab.setStatus("ACTIVE");
685|         lab.setCreateTime(new Date());
686|         lab.setUpdateTime(new Date());
687|         labMapper.insert(lab);
688|     }
689|
690|     public void updateLab(Long id, LabUpdateDto labUpdateDto) {
691|         Lab lab = labMapper.selectById(id);
692|         if (lab == null) {
693|             throw new RuntimeException("实验室不存在");
694|         }
695|
696|         BeanUtils.copyProperties(labUpdateDto, lab);
697|         lab.setUpdateTime(new Date());
698|         labMapper.updateById(lab);
699|     }
700|
701|     public void deleteLab(Long id) {
702|         Lab lab = labMapper.selectById(id);
703|         if (lab == null) {
704|             throw new RuntimeException("实验室不存在");
705|         }
706|
707|         labMapper.deleteById(id);
708|     }
709| }
710| ```
711|
712| ```java
713| @Data
714| public class LabDto {
715|     private Long id;
716|     private String name;
717|     private Long collegeId;
718|     private String collegeName;
719|     private Long managerId;
720|     private String managerName;
721|     private String description;
722|     private String status;
723|     private Date createTime;
724|     private Date updateTime;
725| }
726| ```
727|
728| ```java
729| @Data
730| public class LabCreateDto {
731|     private String name;
732|     private Long collegeId;
733|     private Long managerId;
734|     private String description;
735| }
736| ```
737|
738| ```java
739| @Data
740| public class LabUpdateDto {
741|     private String name;
742|     private Long collegeId;
743|     private Long managerId;
744|     private String description;
745|     private String status;
746| }
747| ```
748|
749| ```vue
750| <template>
751|   <div class="member-container">
752|     <el-card class="member-card">
753|       <template #header>
754|         <div class="member-header">
755|           <h2>成员管理</h2>
756|           <el-button type="primary" @click="openAddDialog">添加成员</el-button>
757|         </div>
758|       </template>
759|       <el-input
760|         v-model="searchQuery"
761|         placeholder="搜索成员姓名"
762|         prefix-icon="el-icon-search"
763|         style="margin-bottom: 16px"
764|         @keyup.enter="loadMemberList"
765|       />
766|       <el-table :data="memberList" style="width: 100%">
767|         <el-table-column prop="id" label="成员ID" width="100" />
768|         <el-table-column prop="name" label="姓名" />
769|         <el-table-column prop="role" label="角色" width="120">
770|           <template #default="{ row }">
771|             <el-tag :type="row.role === 'manager' ? 'primary' : 'default'">
772|               {{ row.role === 'manager' ? '管理员' : '成员' }}
773|             </el-tag>
774|           </template>
775|         </el-table-column>
776|         <el-table-column prop="joinTime" label="加入时间" width="180">
777|           <template #default="{ row }">
778|             {{ formatDateTime(row.joinTime) }}
779|           </template>
780|         </el-table-column>
781|         <el-table-column label="操作" width="200">
782|           <template #default="{ row }">
783|             <el-button size="small" @click="editRole(row.id)">修改角色</el-button>
784|             <el-button size="small" type="danger" @click="removeMember(row.id)">移除</el-button>
785|           </template>
786|         </el-table-column>
787|       </el-table>
788|       <div class="pagination" v-if="total > 0">
789|         <el-pagination
790|           v-model:current-page="currentPage"
791|           v-model:page-size="pageSize"
792|           :page-sizes="[10, 20, 50, 100]"
793|           layout="total, sizes, prev, pager, next, jumper"
794|           :total="total"
795|           @size-change="handleSizeChange"
796|           @current-change="handleCurrentChange"
797|         />
798|       </div>
799|     </el-card>
800|
801|     <!-- 添加成员对话框 -->
802|     <el-dialog v-model="addDialogVisible" title="添加成员">
803|       <el-form :model="addForm" :rules="addRules" ref="addFormRef" label-width="100px">
804|         <el-form-item label="用户ID" prop="userId">
805|           <el-input v-model="addForm.userId" placeholder="请输入用户ID" />
806|         </el-form-item>
807|         <el-form-item label="角色" prop="role">
808|           <el-select v-model="addForm.role" placeholder="选择角色">
809|             <el-option label="管理员" value="manager" />
810|             <el-option label="成员" value="member" />
811|           </el-select>
812|         </el-form-item>
813|       </el-form>
814|       <template #footer>
815|         <span class="dialog-footer">
816|           <el-button @click="addDialogVisible = false">取消</el-button>
817|           <el-button type="primary" @click="submitAdd">确定</el-button>
818|         </span>
819|       </template>
820|     </el-dialog>
821|
822|     <!-- 修改角色对话框 -->
823|     <el-dialog v-model="editRoleDialogVisible" title="修改角色">
824|       <el-form :model="editRoleForm" :rules="editRoleRules" ref="editRoleFormRef" label-width="100px">
825|         <el-form-item label="角色" prop="role">
826|           <el-select v-model="editRoleForm.role" placeholder="选择角色">
827|             <el-option label="管理员" value="manager" />
828|             <el-option label="成员" value="member" />
829|           </el-select>
830|         </el-form-item>
831|       </el-form>
832|       <template #footer>
833|         <span class="dialog-footer">
834|           <el-button @click="editRoleDialogVisible = false">取消</el-button>
835|           <el-button type="primary" @click="submitEditRole">确定</el-button>
836|         </span>
837|       </template>
838|     </el-dialog>
839|   </div>
840| </template>
841|
842| <script setup>
843| import { ref, reactive, onMounted } from 'vue'
844| import { ElMessage } from 'element-plus'
845| import dayjs from 'dayjs'
846| import { getMemberList, addMember, updateMemberRole, removeMember as removeMemberApi } from '@/api/labMembers'
847|
848| const memberList = ref([])
849| const total = ref(0)
850| const currentPage = ref(1)
851| const pageSize = ref(10)
852| const searchQuery = ref('')
853| const addDialogVisible = ref(false)
854| const editRoleDialogVisible = ref(false)
855| const addFormRef = ref(null)
856| const editRoleFormRef = ref(null)
857| const currentMemberId = ref(null)
858|
859| const addForm = reactive({
860|   userId: '',
861|   role: 'member'
862| })
863|
864| const editRoleForm = reactive({
865|   role: ''
866| })
867|
868| const addRules = {
869|   userId: [
870|     { required: true, message: '请输入用户ID', trigger: 'blur' }
871|   ],
872|   role: [
873|     { required: true, message: '请选择角色', trigger: 'change' }
874|   ]
875| }
876|
877| const editRoleRules = {
878|   role: [
879|     { required: true, message: '请选择角色', trigger: 'change' }
880|   ]
881| }
882|
883| const loadMemberList = async () => {
884|   try {
885|     const response = await getMemberList({
886|       page: currentPage.value,
887|       size: pageSize.value,
888|       name: searchQuery.value
889|     })
890|     memberList.value = response.data.list
891|     total.value = response.data.total
892|   } catch (error) {
893|     ElMessage.error('获取成员列表失败')
894|   }
895| }
896|
897| const handleSizeChange = (size) => {
898|   pageSize.value = size
899|   loadMemberList()
900| }
901|
902| const handleCurrentChange = (current) => {
903|   currentPage.value = current
904|   loadMemberList()
905| }
906|
907| const openAddDialog = () => {
908|   addDialogVisible.value = true
909| }
910|
911| const submitAdd = async () => {
912|   if (!addFormRef.value) return
913|   await addFormRef.value.validate(async (valid) => {
914|     if (valid) {
915|       try {
916|         await addMember(addForm)
917|         ElMessage.success('成员添加成功')
918|         addDialogVisible.value = false
919|         loadMemberList()
920|       } catch (error) {
921|         ElMessage.error('成员添加失败')
922|       }
923|     }
924|   })
925| }
926|
927| const editRole = (id) => {
928|   currentMemberId.value = id
929|   editRoleDialogVisible.value = true
930| }
931|
932| const submitEditRole = async () => {
933|   if (!editRoleFormRef.value) return
934|   await editRoleFormRef.value.validate(async (valid) => {
935|     if (valid) {
936|       try {
937|         await updateMemberRole(currentMemberId.value, editRoleForm)
938|         ElMessage.success('角色修改成功')
939|         editRoleDialogVisible.value = false
940|         loadMemberList()
941|       } catch (error) {
942|         ElMessage.error('角色修改失败')
943|       }
944|     }
945|   })
946| }
947|
948| const removeMember = async (id) => {
949|   try {
950|     await removeMemberApi(id)
951|     ElMessage.success('成员移除成功')
952|     loadMemberList()
953|   } catch (error) {
954|     ElMessage.error('成员移除失败')
955|   }
956| }
957|
958| const formatDateTime = (date) => {
959|   return date ? dayjs(date).format('YYYY-MM-DD HH:mm:ss') : '-'
960| }
961|
962| onMounted(() => {
963|   loadMemberList()
964| })
965| </script>
966|
967| <style scoped>
968| .member-container {
969|   padding: 16px;
970| }
971|
972| .member-header {
973|   display: flex;
974|   justify-content: space-between;
975|   align-items: center;
976|   margin-bottom: 20px;
977| }
978|
979| .member-header h2 {
980|   margin: 0;
981|   color: #1b1f2a;
982| }
983|
984| .pagination {
985|   margin-top: 20px;
986|   display: flex;
987|   justify-content: flex-end;
988| }
989| </style>
990| ```
991|
992| ```java
993| @RestController
994| @RequestMapping("/api/labMembers")
995| public class LabMemberController {
996|
997|     @Autowired
998|     private LabMemberService labMemberService;
999|
1000|     @GetMapping
1001|     public Result<PageInfo<LabMemberDto>> getMemberList(
1002|             @RequestParam(defaultValue = "1") Integer page,
1003|             @RequestParam(defaultValue = "10") Integer size,
1004|             @RequestParam(required = false) String name) {
1005|         PageInfo<LabMemberDto> pageInfo = labMemberService.getMemberList(page, size, name);
1006|         return Result.success(pageInfo);
1007|     }
1008|
1009|     @PostMapping
1010|     public Result<Void> addMember(@RequestBody LabMemberAddDto labMemberAddDto) {
1011|         labMemberService.addMember(labMemberAddDto);
1012|         return Result.success();
1013|     }
1014|
1015|     @PutMapping("/{id}/role")
1016|     public Result<Void> updateMemberRole(@PathVariable Long id, @RequestBody LabMemberRoleUpdateDto labMemberRoleUpdateDto) {
1017|         labMemberService.updateMemberRole(id, labMemberRoleUpdateDto.getRole());
1018|         return Result.success();
1019|     }
1020|
1021|     @DeleteMapping("/{id}")
1022|     public Result<Void> removeMember(@PathVariable Long id) {
1023|         labMemberService.removeMember(id);
1024|         return Result.success();
1025|     }
1026| }
1027| ```
1028|
1029| ```java
1030| @Service
1031| public class LabMemberService {
1032|
1033|     @Autowired
1034|     private LabMemberMapper labMemberMapper;
1035|
1036|     @Autowired
1037|     private UserMapper userMapper;
1038|
1039|     public PageInfo<LabMemberDto> getMemberList(Integer page, Integer size, String name) {
1040|         PageHelper.startPage(page, size);
1041|         List<LabMember> members = labMemberMapper.selectList(new QueryWrapper<LabMember>());
1042|         PageInfo<LabMember> pageInfo = new PageInfo<>(members);
1043|
1044|         List<LabMemberDto> memberDtos = members.stream().map(member -> {
1045|             LabMemberDto dto = new LabMemberDto();
1046|             BeanUtils.copyProperties(member, dto);
1047|
1048|             // 获取用户信息
1049|             User user = userMapper.selectById(member.getUserId());
1050|             if (user != null) {
1051|                 dto.setName(user.getName());
1052|                 dto.setUsername(user.getUsername());
1053|             }
1054|
1055|             return dto;
1056|         }).filter(dto -> {
1057|             // 过滤名称
1058|             if (StringUtils.isBlank(name)) {
1059|                 return true;
1060|             }
1061|             return dto.getName() != null && dto.getName().contains(name);
1062|         }).collect(Collectors.toList());
1063|
1064|         PageInfo<LabMemberDto> result = new PageInfo<>();
1065|         BeanUtils.copyProperties(pageInfo, result);
1066|         result.setList(memberDtos);
1067|         return result;
1068|     }
1069|
1070|     public void addMember(LabMemberAddDto labMemberAddDto) {
1071|         LabMember labMember = new LabMember();
1072|         BeanUtils.copyProperties(labMemberAddDto, labMember);
1073|         labMember.setJoinTime(new Date());
1074|         labMemberMapper.insert(labMember);
1075|     }
1076|
1077|     public void updateMemberRole(Long id, String role) {
1078|         LabMember labMember = labMemberMapper.selectById(id);
1079|         if (labMember == null) {
1080|             throw new RuntimeException("成员不存在");
1081|         }
1082|
1083|         labMember.setRole(role);
1084|         labMemberMapper.updateById(labMember);
1085|     }
1086|
1087|     public void removeMember(Long id) {
1088|         LabMember labMember = labMemberMapper.selectById(id);
1089|         if (labMember == null) {
1090|             throw new RuntimeException("成员不存在");
1091|         }
1092|
1093|         labMemberMapper.deleteById(id);
1094|     }
1095| }
1096| ```
1097|
1098| ```java
1099| @Data
1100| public class LabMemberDto {
1101|     private Long id;
1102|     private Long labId;
1103|     private Long userId;
1104|     private String name;
1105|     private String username;
1106|     private String role;
1107|     private Date joinTime;
1108| }
1109| ```
1110|
1111| ```java
1112| @Data
1113| public class LabMemberAddDto {
1114|     private Long labId;
1115|     private Long userId;
1116|     private String role;
1117| }
1118| ```
1119|
1120| ```java
1121| @Data
1122| public class LabMemberRoleUpdateDto {
1123|     private String role;
1124| }
1125| ```
1126|
1127| ```vue
1128| <template>
1129|   <div class="statistics-container">
1130|     <el-card class="statistics-card">
1131|       <template #header>
1132|         <div class="statistics-header">
1133|           <h2>统计分析</h2>
1134|           <el-date-picker
1135|             v-model="dateRange"
1136|             type="daterange"
1137|             range-separator="至"
1138|             start-placeholder="开始日期"
1139|             end-placeholder="结束日期"
1140|             @change="loadStatistics"
1141|           />
1142|         </div>
1143|       </template>
1144|       <div class="stats-grid">
1145|         <el-card class="stat-card" v-for="stat in stats" :key="stat.label">
1146|           <div class="stat-value">{{ stat.value }}</div>
1147|           <div class="stat-label">{{ stat.label }}</div>
1148|           <div class="stat-desc">{{ stat.desc }}</div>
1149|         </el-card>
1150|       </div>
1151|       <div class="chart-container">
1152|         <el-card class="chart-card">
1153|           <template #header>
1154|             <h3>实验室活跃度趋势</h3>
1155|           </template>
1156|           <div ref="activityChartRef" class="chart"></div>
1157|         </el-card>
1158|         <el-card class="chart-card">
1159|           <template #header>
1160|             <h3>成员增长趋势</h3>
1161|           </template>
1162|           <div ref="memberChartRef" class="chart"></div>
1163|         </el-card>
1164|       </div>
1165|     </el-card>
1166|   </div>
1167| </template>
1168|
1169| <script setup>
1170| import { ref, onMounted, onUnmounted } from 'vue'
1171| import { ElMessage } from 'element-plus'
1172| import * as echarts from 'echarts'
1173| import { getStatistics, getActivityTrend, getMemberTrend } from '@/api/statistics'
1174|
1175| const dateRange = ref([new Date(Date.now() - 30 * 24 * 60 * 60 * 1000), new Date()])
1176| const stats = ref([])
1177| const activityChartRef = ref(null)
1178| const memberChartRef = ref(null)
1179| let activityChart = null
1180| let memberChart = null
1181|
1182| const loadStatistics = async () => {
1183|   try {
1184|     const response = await getStatistics({
1185|       startDate: dateRange.value[0],
1186|       endDate: dateRange.value[1]
1187|     })
1188|     stats.value = response.data
1189|   } catch (error) {
1190|     ElMessage.error('获取统计数据失败')
1191|   }
1192| }
1193|
1194| const loadActivityTrend = async () => {
1195|   try {
1196|     const response = await getActivityTrend({
1197|       startDate: dateRange.value[0],
1198|       endDate: dateRange.value[1]
1199|     })
1200|     const data = response.data
1201|     if (activityChart) {
1202|       activityChart.setOption({
1203|         xAxis: {
1204|           type: 'category',
1205|           data: data.dates
1206|         },
1207|         yAxis: {
1208|           type: 'value'
1209|         },
1210|         series: [{
1211|           data: data.values,
1212|           type: 'line',
1213|           smooth: true
1214|         }]
1215|       })
1216|     }
1217|   } catch (error) {
1218|     ElMessage.error('获取活跃度趋势失败')
1219|   }
1220| }
1221|
1222| const loadMemberTrend = async () => {
1223|   try {
1224|     const response = await getMemberTrend({
1225|       startDate: dateRange.value[0],
1226|       endDate: dateRange.value[1]
1227|     })
1228|     const data = response.data
1229|     if (memberChart) {
1230|       memberChart.setOption({
1231|         xAxis: {
1232|           type: 'category',
1233|           data: data.dates
1234|         },
1235|         yAxis: {
1236|           type: 'value'
1237|         },
1238|         series: [{
1239|           data: data.values,
1240|           type: 'line',
1241|           smooth: true
1242|         }]
1243|       })
1244|     }
1245|   } catch (error) {
1246|     ElMessage.error('获取成员增长趋势失败')
1247|   }
1248| }
1249|
1250| const initCharts = () => {
1251|   if (activityChartRef.value) {
1252|     activityChart = echarts.init(activityChartRef.value)
1253|   }
1254|   if (memberChartRef.value) {
1255|     memberChart = echarts.init(memberChartRef.value)
1256|   }
1257| }
1258|
1259| const handleResize = () => {
1260|   activityChart?.resize()
1261|   memberChart?.resize()
1262| }
1263|
1264| onMounted(async () => {
1265|   initCharts()
1266|   window.addEventListener('resize', handleResize)
1267|   await loadStatistics()
1268|   await loadActivityTrend()
1269|   await loadMemberTrend()
1270| })
1271|
1272| onUnmounted(() => {
1273|   window.removeEventListener('resize', handleResize)
1274|   activityChart?.dispose()
1275|   memberChart?.dispose()
1276| })
1277| </script>
1278|
1279| <style scoped>
1280| .statistics-container {
1281|   padding: 16px;
1282| }
1283|
1284| .statistics-header {
1285|   display: flex;
1286|   justify-content: space-between;
1287|   align-items: center;
1288|   margin-bottom: 20px;
1289| }
1290|
1291| .statistics-header h2 {
1292|   margin: 0;
1293|   color: #1b1f2a;
1294| }
1295|
1296| .stats-grid {
1297|   display: grid;
1298|   grid-template-columns: repeat(4, 1fr);
1299|   gap: 16px;
1300|   margin-bottom: 24px;
1301| }
1302|
1303| .stat-card {
1304|   text-align: center;
1305|   padding: 20px;
1306| }
1307|
1308| .stat-value {
1309|   font-size: 32px;
1310|   font-weight: 600;
1311|   color: #1b1f2a;
1312|   margin-bottom: 8px;
1313| }
1314|
1315| .stat-label {
1316|   font-size: 16px;
1317|   color: #4b5563;
1318|   margin-bottom: 4px;
1319| }
1320|
1321| .stat-desc {
1322|   font-size: 12px;
1323|   color: #9ca3af;
1324| }
1325|
1326| .chart-container {
1327|   display: grid;
1328|   grid-template-columns: 1fr 1fr;
1329|   gap: 16px;
1330| }
1331|
1332| .chart-card {
1333|   padding: 16px;
1334| }
1335|
1336| .chart-card h3 {
1337|   margin: 0 0 16px 0;
1338|   font-size: 16px;
1339|   color: #1b1f2a;
1340| }
1341|
1342| .chart {
1343|   height: 300px;
1344| }
1345| </style>
1346| ```
1347|
1348| ```java
1349| @RestController
1350| @RequestMapping("/api/statistics")
1351| public class StatisticsController {
1352|
1353|     @Autowired
1354|     private StatisticsService statisticsService;
1355|
1356|     @GetMapping
1357|     public Result<List<StatDto>> getStatistics(
1358|             @RequestParam @DateTimeFormat(pattern = "yyyy-MM-dd") Date startDate,
1359|             @RequestParam @DateTimeFormat(pattern = "yyyy-MM-dd") Date endDate) {
1360|         List<StatDto> stats = statisticsService.getStatistics(startDate, endDate);
1361|         return Result.success(stats);
1362|     }
1363|
1364|     @GetMapping("/activity-trend")
1365|     public Result<TrendDto> getActivityTrend(
1366|             @RequestParam @DateTimeFormat(pattern = "yyyy-MM-dd") Date startDate,
1367|             @RequestParam @DateTimeFormat(pattern = "yyyy-MM-dd") Date endDate) {
1368|         TrendDto trend = statisticsService.getActivityTrend(startDate, endDate);
1369|         return Result.success(trend);
1370|     }
1371|
1372|     @GetMapping("/member-trend")
1373|     public Result<TrendDto> getMemberTrend(
1374|             @RequestParam @DateTimeFormat(pattern = "yyyy-MM-dd") Date startDate,
1375|             @RequestParam @DateTimeFormat(pattern = "yyyy-MM-dd") Date endDate) {
1376|         TrendDto trend = statisticsService.getMemberTrend(startDate, endDate);
1377|         return Result.success(trend);
1378|     }
1379| }
1380| ```
1381|
1382| ```java
1383| @Service
1384| public class StatisticsService {
1385|
1386|     @Autowired
1387|     private LabMapper labMapper;
1388|
1389|     @Autowired
1390|     private LabMemberMapper labMemberMapper;
1391|
1392|     @Autowired
1393|     private AttendanceMapper attendanceMapper;
1394|
1395|     public List<StatDto> getStatistics(Date startDate, Date endDate) {
1396|         List<StatDto> stats = new ArrayList<>();
1397|
1398|         // 实验室数量
1399|         long labCount = labMapper.selectCount(null);
1400|         stats.add(new StatDto("实验室数量", labCount, "当前平台所有实验室总数"));
1401|
1402|         // 成员总数
1403|         long memberCount = labMemberMapper.selectCount(null);
1404|         stats.add(new StatDto("成员总数", memberCount, "所有实验室成员总数"));
1405|
1406|         // 考勤次数
1407|         long attendanceCount = attendanceMapper.selectCount(new QueryWrapper<Attendance>()
1408|                 .between("create_time", startDate, endDate));
1409|         stats.add(new StatDto("考勤次数", attendanceCount, "所选时间段内的考勤总次数"));
1410|
1411|         // 活跃实验室数
1412|         long activeLabCount = labMapper.selectCount(new QueryWrapper<Lab>()
1413|                 .eq("status", "ACTIVE"));
1414|         stats.add(new StatDto("活跃实验室", activeLabCount, "当前状态为活跃的实验室数量"));
1415|
1416|         return stats;
1417|     }
1418|
1419|     public TrendDto getActivityTrend(Date startDate, Date endDate) {
1420|         // 生成日期列表
1421|         List<String> dates = new ArrayList<>();
1422|         List<Long> values = new ArrayList<>();
1423|
1424|         // 计算日期范围内的每天活跃度
1425|         Date currentDate = startDate;
1426|         while (!currentDate.after(endDate)) {
1427|             dates.add(new SimpleDateFormat("yyyy-MM-dd").format(currentDate));
1428|             
1429|             // 统计当天的考勤次数
1430|             long count = attendanceMapper.selectCount(new QueryWrapper<Attendance>()
1431|                     .between("create_time", currentDate, new Date(currentDate.getTime() + 24 * 60 * 60 * 1000)));
1432|             values.add(count);
1433|
1434|             // 增加一天
1435|             currentDate = new Date(currentDate.getTime() + 24 * 60 * 60 * 1000);
1436|         }
1437|
1438|         return new TrendDto(dates, values);
1439|     }
1440|
1441|     public TrendDto getMemberTrend(Date startDate, Date endDate) {
1442|         // 生成日期列表
1443|         List<String> dates = new ArrayList<>();
1444|         List<Long> values = new ArrayList<>();
1445|
1446|         // 计算日期范围内的每天成员数
1447|         Date currentDate = startDate;
1448|         while (!currentDate.after(endDate)) {
1449|             dates.add(new SimpleDateFormat("yyyy-MM-dd").format(currentDate));
1450|             
1451|             // 统计当天的成员数
1452|             long count = labMemberMapper.selectCount(new QueryWrapper<LabMember>()
1453|                     .le("join_time", currentDate));
1454|             values.add(count);
1455|
1456|             // 增加一天
1457|             currentDate = new Date(currentDate.getTime() + 24 * 60 * 60 * 1000);
1458|         }
1459|
1460|         return new TrendDto(dates, values);
1461|     }
1462| }
1463| ```
1464|
1465| ```java
1466| @Data
1467| public class StatDto {
1468|     private String label;
1469|     private long value;
1470|     private String desc;
1471|
1472|     public StatDto(String label, long value, String desc) {
1473|         this.label = label;
1474|         this.value = value;
1475|         this.desc = desc;
1476|     }
1477| }
1478| ```
1479|
1480| ```java
1481| @Data
1482| public class TrendDto {
1483|     private List<String> dates;
1484|     private List<Long> values;
1485|
1486|     public TrendDto(List<String> dates, List<Long> values) {
1487|         this.dates = dates;
1488|         this.values = values;
1489|     }
1490| }
1491| ```